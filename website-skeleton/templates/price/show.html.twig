{% extends 'base.html.twig' %}

{% block title %}Price{% endblock %}

{% block body %}
    <h1>Price</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price.id }}</td>
            </tr>
            <tr>
                <th>Person</th>
                <td>{{ price.person }}</td>
            </tr>
            <tr>
                <th>Price_week</th>
                <td>{{ price.priceWeek }}</td>
            </tr>
            <tr>
                <th>Price_wednesday</th>
                <td>{{ price.priceWednesday }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('price_index') }}">back to list</a>

    <a href="{{ path('price_edit', {'id': price.id}) }}">edit</a>

    {{ include('price/_delete_form.html.twig') }}
{% endblock %}
