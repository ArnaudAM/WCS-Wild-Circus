<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        <title>{% block title %}Wild Circus{% endblock %}</title>

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

    </head>
    <body>

    <nav class="navbar navbar-dark bg-dark">
        <div class="navbar" id="navbarNavDropdown">
            <ul class="navbar-pers">
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('performance_index') }}">Performances</a>
                    {% else %}
                        <a class="nav-link" href="{{ path('performance') }}">Performances</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('about_us_index') }}">About us</a>
                    {% else %}
                    <a class="nav-link" href="{{ path('about_us') }}">About us</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('price_index') }}">Prices</a>
                    {% else %}
                    <a class="nav-link" href="{{ path('prices') }}">Prices</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                    {% else %}
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                    {% endif %}
                </li>
            </ul>
        </div>
        <p>
            {% if app.user %}
                Bonjour {{ app.user.email }} !
                <a href="{{ path('app_logout') }}"> Se déconnecter</a>
            {% else %}
                <a href="{{ path('app_login') }}"> Se connecter</a>
            {% endif %}
        </p>
    </nav>

        {% block body %}{% endblock %}

    <footer>
        <ul>
            <li><a href="https://wildcodeschool.fr/" target="blank">Wild Circus</a> by <a href="" class="lienpied" target="_blank">[Arnaud Auzou]</a></li>
            <li>GitHub: <a href="https://github.com/ArnaudAM" target="_blank">ArnaudAM</a></li>
            <li>LinkeDin: <a href="https://www.linkedin.com/in/arnaud-auzou-de-marichalar/" target="_blank">Arnaud Auzou</a></li>
            <li>Wild Code School - [17/07/2019]</li>
        </ul>
    </footer>

        {% block javascripts %}{% endblock %}
    </body>
</html>
